import React from 'react';

const ReportMenu = () => {
  return (
    <div className="report-menu">
      <button>Combined</button>
      <button>Route</button>
      <button>Events</button>
      <button>Trips</button>
      <button>Stops</button>
      <button>Summary</button>
      {/* Add other report buttons here */}
    </div>
  );
};

export default ReportMenu;
